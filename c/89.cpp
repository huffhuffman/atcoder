#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

bool isMarch(char c) {
  return c == 'M' || c == 'A' || c == 'R' || c == 'C' || c == 'H';
}

int main() {
  cin.tie(0);
  ios::sync_with_stdio(false);

  int n;
  cin >> n;

  vector<string> s(n);
  for (int i = 0; i < n; i++) {
    cin >> s.at(i);
  }

  ll ans;

  return 0;
}