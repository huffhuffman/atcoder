// const stdin = require('fs').readFileSync('/dev/stdin', 'utf8')

const stdin = `6 4
-10 8 2 1 2 6`

const [n, k] = stdin
  .split('\n')[0]
  .split(' ')
  .map(Number)
const v = stdin
  .trim()
  .split('\n')[1]
  .split(' ')
  .map(Number)

const ans = _ => {
  
}
